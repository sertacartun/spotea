{
  "schemaVersion": 2,
  "evidenceRunId": null,
  "evidenceSummary": {
    "schemaVersion": 1,
    "status": "fail",
    "gates": {
      "code": "blocked",
      "visual": "blocked",
      "temporal": "blocked",
      "human": "blocked"
    }
  },
  "risks": [
    "No gate is marked pass here because the evidence verifier was never run - only it may derive a pass, and claiming one by hand would be exactly the fabricated verification the pipeline forbids. What was actually done, and can be re-checked: the project's own suite (706 tests) and ruff both pass; styleseed-check reports 4 warnings for the whole project, none of them in any file this work wrote; and every surface below was rendered in a real browser at 390x844 and inspected.",
    "Rendered and inspected: home (loaded, empty, scrolled, desktop), library, explore, detail, settings, player-overlay, playlist-picker, downloads, interests, auth login and register. Not verified: refresh-progress - its trigger lives in the mobile menu and was not reachable in the automated pass.",
    "Temporal: V1 (ambient tint cross-fade) is recorded from the running app and the tint provably changes, rgb(59 49 35) -> rgb(50 43 27). V2 (mini-to-full player) is blocked because that transition is not built - expanding still toggles the overlay's hidden attribute.",
    "Two of the three standard shelves below Home's lead shelf still share a composition; the lead-shelf variation only breaks the first repeat.",
    "Work is uncommitted in the working tree of branch fix/sync-must-not-starve-playback at the user's instruction. app/static/js/pages/index.js is touched by both that branch's commit and this work, so a later branch move needs a manual resolve on that file."
  ]
}
